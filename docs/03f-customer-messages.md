# 03F 客户端「消息中心 + 微信通知」模块

> 状态：已确认子模块
> 所属阶段：03 客户端微信小程序
> 版本：V1.0

## 1. 设计原则

- 不新增底部一级导航，入口位于首页右上角及“我的 → 消息”，两个入口进入同一消息中心。
- 所有重要业务事件优先自动生成站内消息；真正需要客户及时知道的事件再尝试发送微信通知。
- 绝大多数通知由业务状态变化自动触发，不要求店员额外点击“发送通知”。
- 微信通知属于附加通知渠道，发送失败不得影响预约、订单、服务、余额、优惠券等正式业务状态。
- V1.0 不做即时聊天系统，不做在线客服系统，不做营销群发。
- 消息点击后应直接进入对应业务详情页，避免客户再次手工查找。

## 2. 消息中心

入口：首页右上角 / 我的 → 消息。

消息中心采用单一时间流，不设计复杂多 Tab。消息可标识业务类型，例如：预约、服务、寄养、商品、钱包 / 会员资产、宠物交友、上门喂养、平台积分。

每条消息至少保存：

- scope：`platform`（平台个人消息）/ `merchant`（门店个人消息）
- recipient_user_id：平台个人消息必填，指向明确接收用户；门店消息在 Customer 尚未安全绑定 User 前允许为空，必须保留明确的 merchant_id + customer_id，不猜测接收账号
- merchant_id / customer_id：门店消息必填；平台消息不以这两个字段作为接收人或可见性依据
- related_store_id：平台业务关联的绑定 / 领取门店（如有），仅表达业务关联，不自动授予门店私有数据权限
- message_type
- title
- summary / content
- related_business_type
- related_business_id
- priority
- read_status
- created_at
- read_at（可选）
- jump_target / 跳转目标

门店在客户首次使用小程序前产生的业务消息，可先关联该门店 Customer。按 03g 完成安全绑定后，服务端校验当前 User 与该 Customer 的有效关系，才允许读取 / 关联待识别消息；不因相同手机号、扫描凭证或候选匹配而提前暴露内容。已明确属于其他 User 的消息不得自动改接收人；换绑 / 合并按正式身份规则处理，保留审计。平台消息始终需要明确的接收 User。

客户端支持未读数量展示及“全部标记已读”。V1.0 不提供客户端删除业务消息能力。

### 2.1 展示范围与切店

- 当前时间流由“当前 User 的平台个人消息 + 当前 Merchant 下属于当前 User / Customer 的门店个人消息”组成。
- 切换门店后保留本人的平台个人消息，仅替换门店消息部分。没有有效当前门店时，只能展示本人的平台个人消息，并提供选择门店入口。
- 其他门店私有消息不混入当前门店列表。上门喂养和平台积分消息不会因为用户切店而消失；绑定门店名称可作为业务说明展示。
- 服务端分别校验接收用户、消息作用域和目标业务权限；不得为了读取平台消息而取消门店消息的租户过滤。

### 2.2 未读与已读

- 首页和“我的”的消息未读数采用同一可见范围：本人平台未读 + 当前门店本人未读，不包含其他门店私有消息。
- “全部标记已读”只作用于操作时当前 User、当前门店和该次操作截止时间之前的可见消息；操作期间切店或新增的消息不得被扩大范围误标。
- 点击消息并成功打开消息内容或有权限的业务详情后标记已读；仅滚过列表不标记。读取失败、权限校验失败时不因前端已点击就写入已读。
- 已读状态由服务端保存，重复操作幂等；切店不重置历史已读状态。

## 3. 预约消息

以下事件自动生成站内消息：

- 预约提交成功
- 门店确认预约
- 门店建议修改时间
- 预约拒绝
- 预约取消
- 客户修改预约后重新进入待确认

微信通知优先用于需要客户及时处理或知晓的预约变化，例如：门店确认、建议新时间、拒绝、取消。

## 4. 洗护 / 美容消息

客户侧不对员工内部每个服务步骤逐条推送。

V1.0 主要消息节点：

- 服务开始
- 服务完成

其中“服务完成”为重要通知，应优先尝试微信通知。

服务完成消息直接跳转本次服务结果页，可查看 N 组前后照片对比，并由客户主动决定是否查看 AI 洗护报告。

**服务完成本身不启动 AI 分析，也不把本次服务照片提交给 AI Provider。**

客户在服务结果页点击“查看报告”，完成有效广告观看 / 获得有效查看资格后，后端才创建 AI 分析任务并提交本次可用于分析的前后照片。

如果客户仍停留在结果页，客户端显示与真实处理流程对应的阶段状态：

1. 宠物影像质量检测中
2. 洗护前后特征比对中
3. 毛发与清洁状态分析中
4. 专业护理知识检索中
5. 结果复核与报告生成中

AI 结果一旦实际完成，应立即展示，不得为了填满等待动画而故意延迟。

正常体验目标为约 30～60 秒。若约 60 秒后后台任务仍未完成，客户端不继续让客户原地无限等待，改为提示：

> 报告仍在生成中
>
> 分析已提交，可稍后返回本页面查看结果。

AI 报告生成完成后默认不额外重复发送微信通知。客户之后再次进入同一服务结果页时读取服务端真实任务状态：

- 未完成 → 显示仍在生成
- 已完成 → 直接展示已获得查看资格的报告
- 失败 → 显示失败状态与重试入口

若客户已经完成广告并获得查看资格，后续 AI 失败或超时重试不得要求再次观看广告。

## 5. 寄养消息

普通寄养动态不逐条发送微信通知。

适合形成站内消息或重要通知的事件包括：

- 办理入住
- 新增图片 / 视频动态
- 异常情况
- 寄养即将结束 / 已结束

多个短时间内连续新增的普通图片、视频或寄养动态由后端尽量合并提示，例如“旺财有 3 条新的寄养动态”，减少消息轰炸。

寄养异常记录默认勾选“通知客户”；店员可在仅作内部记录时主动取消通知。

异常通知直接进入对应寄养详情 / 时间轴。

## 6. 商品消息

商品购买需求可产生以下站内消息：

- 购买需求已提交
- 门店已确认
- 门店已调整商品 / 数量
- 订单即将自动取消
- 订单已取消
- 订单已完成

微信通知优先用于：

- 门店已确认，可到店线下付款
- 门店修改商品 / 数量
- 订单即将超时取消
- 门店取消订单

商品订单按已确认的库存预占规则执行。默认 24 小时未完成付款自动取消前，在临近超时时自动提醒客户；自动取消后释放库存并恢复尚未核销的优惠券。

## 7. 钱包 / 会员资产消息

V1.0 已包含独立的平台积分体系。积分消息按平台个人作用域展示，不混入商户钱包账本；详细业务见 `03k-customer-loyalty-points.md`。

钱包 / 会员资产相关消息包括：

- 充值完成
- 余额人工调整
- 优惠券到账
- 邀请码兑换成功
- 优惠券即将过期（低优先级，可选）

充值完成后系统自动生成站内消息，展示本次到账金额、当前余额以及自动赠送的优惠券。

邀请码兑换在当前页面直接展示结果，同时保留站内消息记录；不再额外发送微信通知。

优惠券即将过期默认仅做站内提醒，不主动发送营销性质微信通知。

## 8. 消息跳转

消息必须尽量直接跳到对应业务页面：

- 预约消息 → 预约详情
- 洗护 / 美容完成 → 服务结果页
- 寄养消息 → 寄养时间轴 / 寄养详情
- 商品消息 → 商品订单详情
- 优惠券到账 → 我的优惠券
- 余额变动 → 余额明细
- 宠物好友邀请 → 对应宠物交友邀请处理页
- 宠物好友邀请同意后的状态消息 → 对应交友邀请详情
- 上门喂养状态 → 对应需求详情 / 本人的发布或接单记录
- 积分兑换、到店自提、取消返还 → 对应积分兑换详情 / 积分明细

消息目标必须由服务端关联业务校验，不可信任客户端传入的门店或业务 ID。平台业务详情不因关联门店不同而自动切换当前门店；进入门店私有业务详情时，先以该业务所属门店解析上下文并校验当前 User / Customer 权限，再整体切换门店数据。消息本身不能授予该门店权限。

从微信通知或外部入口打开时，启动封面、登录 / 手机号验证完成后仍须恢复原消息目标，规则见 `03p`、`03g`。目标已取消或结束时展示其真实历史状态；目标不可见、失效或无权限时显示明确提示及返回消息入口，不返回一个看似成功但无关的首页，也不泄露其他客户信息。

### 8.1 平台业务消息事件

| 业务事件 | 接收对象 | scope | 站内结果 / 跳转 |
|---|---|---|---|
| 上门喂养接单成功 | 发布者、有效接单者 | platform | 已接单待门店对接 → 对应需求详情 |
| 上门喂养对接状态更新、有效取消、重新开放或完成 | 本次变化涉及的发布者 / 接单者 | platform | 展示服务端真实状态与对应记录；旧接单者不获得新接单者资料 |
| 积分兑换结果确认 | 兑换用户 | platform | 兑换详情；失败时明确积分是否未扣、处理中或已返还 |
| 实物到店待自提、领取确认 | 兑换用户 | platform | 对应兑换详情与领取门店 |
| 兑换取消返还处理中 / 返还完成 | 兑换用户 | platform | 兑换详情及可查询的积分补偿结果 |

上述事件须在业务结果成立后生成站内消息，事件重复投递不重复创建同一通知。是否尝试微信通知按事件配置和用户可接收条件执行；失败不回滚正式业务。门店内部对接 / 收货待办仍使用店员端权限，不通过平台个人消息开放其他用户资料。

## 9. 主动通知客户

V1.0 为店员保留轻量“通知客户”能力，用于特殊业务情况，不发展为聊天系统。

常用模板至少包括：

- 请联系门店
- 服务预计稍有延迟
- 可以来接宠物了
- 请确认新的预约时间
- 自定义

普通店员只能针对与当前业务相关的单个客户发送通知；群发营销不进入 V1.0。

是否允许普通店员发送自定义通知由权限体系控制。

## 10. 消息优先级

V1.0 按三档处理：

### 重要

例如：预约改时间、服务完成、寄养异常、商品确认 / 取消、收到宠物好友邀请。

- 生成站内消息
- 是否尝试微信通知按事件配置；好友邀请至少必须产生站内消息

### 普通

例如：服务开始、商品完成、充值完成、优惠券到账、好友邀请同意后的“已告知店员”状态通知。

- 生成站内消息
- 微信通知按具体事件决定

### 低优先级

例如：普通寄养动态、优惠券即将过期。

- 仅站内消息或业务页面提示
- 默认不主动微信通知

## 11. 微信通知失败处理

微信通知发送与正式业务状态必须解耦。

例如服务完成后：

1. 服务订单正常进入完成状态。
2. 生成站内消息。
3. 尝试发送微信通知。
4. 即使微信通知失败，订单和服务状态也不得回滚。

后台记录发送结果及失败原因，用于审计和问题排查。

客户未授权或无法接收微信通知时，站内消息、订单状态、服务结果、宠物好友邀请等功能仍应正常工作。

## 12. 通知审计

至少保存：

- 消息类型
- 关联业务对象
- 客户
- 创建时间
- 站内消息状态
- 是否尝试微信通知
- 微信发送时间
- 微信发送成功 / 失败
- 失败原因（如有）

店员主动通知时额外记录：

- 操作员工
- 通知内容 / 模板
- 操作时间

## 13. 宠物交友消息

宠物交友复用现有消息中心，不建立客户之间的聊天会话。

至少包含以下消息：

### 13.1 收到好友邀请

被邀请方收到：

> “{发起方宠物昵称} 想和 {目标宠物昵称} 交个朋友”

消息页直接提供或进入详情后提供：

- 同意
- 不同意

### 13.2 对方同意

被邀请方点击同意后，系统同时通知双方宠物主人：

> 对方已同意，已告知店员，请等待店员回复。

同时创建店员端“宠物交友待处理”任务。

### 13.3 对方不同意 / 不回复

- 不向发起方发送“已拒绝”结果通知。
- 长期未回复也不向发起方发送“未回复”通知。
- 不反复向被邀请方发送催促消息。
- 同一对宠物再次发起邀请时，业务层直接提示“已发送过邀请”，不创建新消息或新邀请。

### 13.4 店员处理

店员后续处理完成后如何通知双方，在阶段 04 店员端小程序中细化；处理结果必须通过现有消息体系发送并留痕。

完整规则见 `03h-customer-pet-social.md`。

## 14. V1.0 不实现

- 即时聊天 / 在线客服
- 客户与店员私聊会话
- 宠物好友双方主人直接聊天
- 营销群发
- 复杂消息订阅中心
- 因客户未开启通知而阻塞正式业务流程

## 15. 需求变更记录

### 2026-09-08：平台消息范围与目标恢复

【需求变更】

- 原规则：消息统一按门店保存和切换，保留“V1.0 无积分”的旧表述，新增平台业务通知的可见范围、未读口径和目标校验未收口。
- 新规则：同一时间流展示本人平台消息与当前门店本人消息；补充 scope、接收人、已读范围、平台事件及深链权限校验，启动 / 登录后恢复原目标；删除无积分旧表述。
- 影响范围：首页 / 我的消息入口、身份与跨店上下文、上门喂养、积分兑换、微信通知、消息 API 和验收。

### 2026-09-01：AI 报告改为客户主动触发生成

【需求变更】

- 原规则：服务完成后可提前生成 AI 报告，客户点击时主要负责广告解锁。
- 新规则：服务完成只生成普通服务结果和前后照片对比，不启动 AI；客户获得报告查看资格后才提交照片并创建 AI 任务；生成过程中显示阶段化处理状态，完成后直接展示结果。
- 影响范围：客户端服务结果页、AI 调用时机、消息、后端任务、API、成本与隐私。

### 2026-09-01：AI 报告等待体验最终确认

【需求变更】

- 原规则：生成中使用“宠物图片对比中 → 专业数据库查询中 → 结果复核中”等 3 段状态，未明确前台最长等待体验。
- 新规则：改为 5 段专业流程文案：宠物影像质量检测 → 洗护前后特征比对 → 毛发与清洁状态分析 → 专业护理知识检索 → 结果复核与报告生成；真实结果提前完成则立即展示，正常目标 30～60 秒，约 60 秒仍未完成则提示客户稍后返回查看。
- 影响范围：客户端服务结果页、消息、AI 任务状态映射、API 与异常体验。
