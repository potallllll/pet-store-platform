# AGENTS.md

本仓库当前处于**需求设计阶段**，不是可自由实现的代码仓库。任何 AI 编码工具、自动化代理或新加入的开发者在执行任务前，必须先阅读本文件与 `docs/README.md`。

## 1. 当前唯一正式需求来源

- 当前阶段、基线状态和待决项只读 `docs/REQUIREMENTS-STATUS.md`。
- 正式文件地图、阅读顺序和职责边界只读 `docs/README.md`。
- 具体业务、页面和 UI 规则只读文件地图指向的正式模块文档，不从本文件推导业务规则。
- `docs/history/` 仅用于追溯，不属于当前正式需求来源。

聊天记录、临时 H5、外部方案、旧 AI 方案、口头描述和其他仓库内容均不是正式规格，除非已同步到当前有效的正式模块文档。

## 2. 冲突处理

- 先读取机器状态字段；只有 `status: confirmed` 的范围可直接作为正式依据，`status: mixed` 必须同时读取 `confirmed_scope`、`pending_scope` 和 `blocked_by`。
- 详细子模块文档用于解释总纲；全局安全、租户隔离、财务审计原则同时生效。
- `03i-customer-screen-blueprint.md` 是当前统一客户端页面地图与关键验收蓝图。
- 原 `03n-customer-screen-blueprint-extension.md` 已归档；其中现行内容已由 `03i` 与对应业务子模块承接，归档文件不得作为实现依据。
- UI 规格不得反向修改业务规则；原型和实现不得覆盖正式模块文档。
- 如果两个已确认文件存在无法同时满足的明确冲突，**不得自行选择、脑补或静默修改**；必须提出冲突并等待需求变更处理。
- `REQUIREMENTS-STATUS.md` 负责阶段与基线摘要，不替代详细业务规格。

## 3. 当前阶段

当前阶段和并行范围以 `docs/REQUIREMENTS-STATUS.md` 顶部字段为唯一来源；本文件不复制阶段结论。

在阶段 13 “V1.0 需求冻结”之前，除非用户明确要求实现原型或代码，否则不要把当前需求文档擅自转成正式产品实现。

## 4. 生成客户端预览 / 原型的强制规则

当任务要求生成：

- 客户端 H5 预览
- 客户端页面 Demo
- 客户端 UI 骨架
- 客户端交互原型
- 从需求文档生成客户端页面

执行者必须按 `docs/README.md` 的“客户端预览 / 原型读取顺序”读取完整上下文，不得自行缩减文件清单，也不得读取已归档的 `03n` 作为实现依据。

必须按照当前 `03i` 主蓝图及已确认详细文档覆盖完整客户端结构。首页预览必须明确区分顶部品牌 Banner、底部 `home_bottom_banner` 广告位和启动前的 `launch_cover_ad`。

临时 H5 只是验收 / 沟通工具，不是正式需求源；如果 H5 与正式 GitHub 文档冲突，应修改 H5。

## 5. 禁止事项

后续进入 Codex / 开发阶段后仍必须遵守：

- 不得删除、弱化或跳过难实现的已冻结需求。
- 不得用 Mock、TODO、静态假数据或仅 UI 演示冒充正式实现；开发 / H5 的广告 Mock 必须明确标注调试用途，正式生产切换真实广告组件。
- 不得绕过租户隔离、权限、Feature Entitlement、服务端校验、财务审计和错误处理。
- 不得让前端直接可信地决定金额、余额、积分、库存、权限、门店付费权益、广告权益或支付结果。
- 不得硬编码密钥、Token、广告 Credential 或散落广告单元配置。
- 不得通过切换门店、直接调用 `pet_advisor` API / placement 等方式绕过养宠顾问付费门店资格。
- 不得因为构建 / 测试失败而注释掉功能或修改业务规则来“过测试”。

## 6. 阅读入口

完整文件索引、文档优先级和当前基线见 `docs/README.md`。

## 7. 变更历史

历史同步记录见 [`docs/history/2026-client-requirements-changes.md`](docs/history/2026-client-requirements-changes.md)；历史表述不得作为当前实现依据。
