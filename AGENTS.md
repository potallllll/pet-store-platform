# AGENTS.md

本仓库当前处于 V1.0 需求设计阶段。AI、Codex 和开发者开始工作前，必须读取本文件、`docs/README.md` 和 `docs/REQUIREMENTS-STATUS.md`，再按任务读取对应权威文档。GitHub 已确认规格是正式来源；聊天、临时 H5、外部方案和历史提交不能自动成为需求。

## 1. 文档职责与读取顺序

- `docs/README.md`：唯一文档地图、职责和阅读顺序。
- `docs/REQUIREMENTS-STATUS.md`：阶段、基线和整改进度，不复制详细业务规则。
- `docs/REQUIREMENTS-DECISIONS.md`：待决事项、确认记录与阻塞关系；提案不等于已确认。
- `docs/00-project-workflow.md`：需求治理、开发和验收流程。
- `docs/01-project-positioning.md`：产品定位及全局边界。
- `docs/02-roles-and-accounts.md`：角色、账号及商户归属。
- `docs/03-customer-miniapp.md`：客户端范围；`03a`～`03m`、`03o`、`03p` 为对应业务权威模块。
- `docs/03i-customer-screen-blueprint.md`：唯一客户端页面地图、跨页面交互及验收 ID 索引。
- `docs/10a-customer-ui-design-system.md`：共享视觉 Token、组件和可用性；`10b` 为首页布局，`10c` 为启动封面 UI。
- `03n-customer-screen-blueprint-extension.md` 已退役为迁移索引，仅供历史追溯，不作为当前实施依据。

涉及客户端全量原型时，读取 03 总纲、所有适用业务模块、03i 和相关 UI 文档；涉及某个模块时，至少读取它的权威文档、直接依赖及适用全局规则。不得仅凭摘要、文件名或原型推断业务行为。

## 2. 冲突与待决处理

只有明确已确认的规则可以作为正式实现依据。具体业务模块负责详细规则，03i 负责页面及验收编号，10a/10b/10c 不反向修改业务。全局租户隔离、权限、安全、财务审计原则始终生效。

两个已确认规则无法同时满足时，停止受影响实现并登记冲突，等待明确需求变更；不得自行选择、脑补、静默覆盖或用“较新文件”自动取代权威规则。每次真正变更记录原规则、新规则、影响范围、确认人及日期、落实提交。历史条款明确标记已被替代并指向现行权威章节，不再作为执行命令。

待决事项必须有唯一 DEC ID、权威位置、负责人、决定状态、阻塞范围和收口阶段。未确认的金额算法、权限、截止时间、运营参数或实现政策不得由 AI 填入正式默认值。已确认的其他独立能力可继续设计和开发准备，但不得声称相关待决场景已通过验收。

## 3. 当前阶段与开发边界

当前主阶段为 04 店员端小程序需求讨论，客户端 UI / Design System 允许提前并行。阶段 03 已有客户端基线，不代表全部跨端规则、数据库、架构或 V1.0 已冻结。正式整体开发按 00 工作流程推进；用户明确要求的独立原型或已确认模块开发准备可以先行，不得依赖未决业务规则自行补全。

管理角色只有 Platform Super Admin、Merchant Owner、Staff。正式称呼为“店员端小程序（老板也可使用）”，不建立独立店长角色。其他完整产品结论只引用对应权威模块，不在本文件维护第二套业务清单。

## 4. 原型与实现要求

- H5、UI 图和模拟数据仅用于沟通与原型验收，必须明确模拟范围；不得把原型成功当作真实服务端或端到端验收通过。
- 正式实现不得以 Mock、TODO、静态假状态或空壳页面冒充功能，不得删除、弱化或跳过困难需求。
- 不得绕过租户隔离、RBAC、Feature Entitlement、服务端校验、财务审计和错误处理。
- 前端不得可信地决定金额、余额、积分、库存、权限、付费权益、广告权益或支付结果。
- 不得硬编码密钥、Token、广告 Credential 或把广告单元配置散落在页面。
- 不得通过切店、直接 API 请求或广告 placement 绕过养宠顾问门店付费资格。
- 不得为了通过构建或测试注释功能、篡改业务规则、伪造测试结果或将未执行测试标为通过。
- 核心收银、余额、优惠券与库存必须满足已确认的一致性和幂等约束；AI、广告、通知、统计等辅助故障不得导致核心经营流程错误成功或重复记账。
- 需求、实现、测试和验收应可追溯。每次交付说明覆盖的需求 ID、真实实现、模拟部分、测试证据、未完成项及阻塞决策。未知结果须如实记录，不能宣称完成。

## 5. 阅读入口

完整文档地图见 `docs/README.md`；当前进度见 `docs/REQUIREMENTS-STATUS.md`；待决事项见 `docs/REQUIREMENTS-DECISIONS.md`。本文件不维护详细业务变更历史。

### 2026-09-08 治理变更

原规则：入口文件重复维护大量客户端业务摘要，并将 03n 当作仍在更新的页面补充。新规则：收窄为 AI 执行约束和权威入口，03n 改为历史迁移索引，待决与验收分别归唯一登记位置。影响范围：全部 AI 阅读、需求变更与后续开发；不改变已确认业务功能或当前主阶段。
